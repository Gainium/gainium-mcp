# 001 — Wire the existing vitest suite into CI

## §1. Behaviour today

Real tests already exist and pass: `npm test` (`vitest run`) runs
`src/__tests__/agent-usability.test.ts` — 1 file, 120 assertions, all
green (§1.1). Nothing runs it in CI — no CI workflow exists at all
(§1.2).

## §2. Desired behaviour

CI runs `npm test` on every PR and fails the check on red (§2.1).

## §3. Open questions / assumptions — none; pure CI wiring on top of an
already-working, already-green suite.
