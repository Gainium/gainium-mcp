# 001 — Wire the existing vitest suite into CI

## Build order

1. Add `.github/workflows/test.yml`: PR + push to `main` (default
   branch), Node 22, `npm ci`, `npm test`.
2. Verify green locally: 1 file, 120 assertions.

## Blast radius

1. Other call sites: none — new CI file only.
2. `core/` submodule: none in this repo.
3. CLAUDE.md contracts: no application code touched.
4. Other tests: none — this is the only suite.

## Traceability

| Spec section | Test / file |
|---|---|
| §2.1 (CI runs the suite, fails on red) | `.github/workflows/test.yml` |
